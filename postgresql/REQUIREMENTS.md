# Requisitos do projeto

Este projeto usa PostgreSQL local no Linux para acompanhar o curso.

## Dependências do sistema

Instalar PostgreSQL:

```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
